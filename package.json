{
  "name": "root-build-scripts",
  "private": true,
  "scripts": {
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm --prefix react-landing install && npm --prefix react-landing run build"
  }
}
